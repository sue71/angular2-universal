describe('app', function() {

  describe('app component', function() {
    beforeEach(function() {
      browser.get('/');
      browser.sleep(2000); // TODO: Remove when problem with preboot is resolved
    });

    it('check if it works', function () {
    });
  });

});
